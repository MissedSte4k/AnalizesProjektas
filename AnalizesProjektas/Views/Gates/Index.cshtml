@model IEnumerable<AnalizesProjektas.Models.Gate>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Vieta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WareHouse.InternalAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TransportType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Vieta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WareHouse.InternalAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TransportType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GateId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GateId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GateId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
